// @kept-hq/evals — case format and runner core.
//
// Hand-written (week 4): the YAML case format, the runner, and every
// assertion type — including the trace-internal ones (cited docs, tool-call
// sequence, no-write-before-confirm). The case format is a product surface,
// not an implementation detail.

export {};
