// @kept-hq/widget — embeddable chat widget (week 1, days 4–5).
//
// UI work, so this one is AI-written against the agent service's streaming
// endpoint and reviewed line by line. React deps land when the widget does;
// nothing is installed ahead of a decision.

export {};
