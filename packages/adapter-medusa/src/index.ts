// @kept-hq/adapter-medusa — Medusa v2 backend (week 3).
//
// Intentionally empty until the `OrderBackend` interface exists in
// @kept-hq/core. The contract is: Dragos designs the interface by hand,
// then the adapter boilerplate gets written against it — not before, or the
// adapter ends up defining the interface by accident.
//
// Acceptance bar: the demo backend and this one pass the same tool tests.

export {};
